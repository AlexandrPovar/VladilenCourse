let expression = [];
const sign = ['>', '<', '=', '+', '-', '*', '/'];

function getMathResult(expression) {
    if (expression.length < 3) {
        alert('Ошибка');
        return;
    }
    expression.forEach( () => {
        
    })
    // return (expression[0] expression[1] expression[3]);
};
console.log(getMathResult([1, +, 5]);