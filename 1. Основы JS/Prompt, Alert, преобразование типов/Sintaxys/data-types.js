const name = 'Alexandr';
console.log('name', name, typeof name);

const age = 31;
console.log('age', age, typeof age);

const programming = true;
console.log('programming', programming, typeof programming);

const studentSurName = null;
console.log('studentSurName', studentSurName, typeof studentSurName);

let studentWork;
console.log('studentWork', typeof studentWork);

const studentInfo = {
    studentName: 'Alexandr',
    studentAge: 31
};
console.log('studentInfo', studentInfo, typeof studentInfo);

const id = Symbol('id');
console.log('Symbol', id, typeof id);

const myFeatureOffer = 1200n;
console.log('myFeatureOffer', myFeatureOffer, typeof myFeatureOffer);
